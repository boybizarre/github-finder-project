import { createContext } from 'react';

const GithubContext = createContext();

export default GithubContext;

// Created the GithubContext first
// Create types
// Create GithubState and functions
// Create github reducer functions with types
// 